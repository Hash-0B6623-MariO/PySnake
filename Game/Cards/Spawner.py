from temp import Base 
import Properties as p

import random

class CardsSpawner:
    
    @classmethod
    def roll_card(cls, group):


    @classmethod
    def spawn_card(cls, card_class:Base):
        ''' Spawns a card of the given class. '''
        return card_class()